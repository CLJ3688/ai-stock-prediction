spring:
  application:
    name: taiex-api
  profiles:
    include: secret
  # ???????
  datasource:
    url: jdbc:mysql://localhost:3306/taiex_analysis?useSSL=false&serverTimezone=Asia/Taipei&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate ??
  jpa:
    hibernate:
      # ??????? Flyway ??????????? validate??????? update ? create?
      ddl-auto: validate
    show-sql: true # ?????????????? SQL ??

  # Flyway ???????
  flyway:
    enabled: true
    baseline-on-migrate: true